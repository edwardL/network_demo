#include "serializestream.h"

static const size_t SIZE_OF_BYTE = sizeof(byte);
static const size_t SIZE_OF_INT	 = sizeof(int);
static const size_t SIZE_OF_SHORT  = sizeof(short);
static const size_t SIZE_OF_LONG64 = sizeof(long long);
static const size_t SIZE_OF_FLOAT  = sizeof(float);
static const size_t SIZE_OF_DOUBLE = sizeof(double);

SerializeStream::SerializeStream()
{

}